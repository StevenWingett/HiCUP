# This is a basic workflow to Test HiCUP

name: HiCUP_Test

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ master, devel, actions ]
  pull_request:
    branches: [ master, devel, actions ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2

    # Runs a single command using the runners shell
    - name: Run a one-line script
      run: echo Hello, world!

    # Runs a set of commands using the runners shell
    - name: Update Ubuntu
      run: |
        sudo apt-get update 

    - name: Install dependencies
      run: |
        sudo apt install samtools
        sudo apt install bowtie2
        sudo apt install r-base-core
        sudo apt install bioperl

    - name: Test all perl scripts
      run: |
        $GITHUB_WORKSPACE/hicup --help
        $GITHUB_WORKSPACE/hicup --version
        $GITHUB_WORKSPACE/hicup_mapper --help
        $GITHUB_WORKSPACE/hicup_digester --help
        $GITHUB_WORKSPACE/hicup_truncater --help
        $GITHUB_WORKSPACE/hicup_deduplicator 2>&1 | grep Please
        $GITHUB_WORKSPACE/Conversion/hicup2hicpipe --help
        $GITHUB_WORKSPACE/Conversion/hicup2gothic --help
        $GITHUB_WORKSPACE/Conversion/hicup2fithic --help
        $GITHUB_WORKSPACE/Conversion/hicup2homer --help
        $GITHUB_WORKSPACE/Conversion/hicup2juicer --help
        $GITHUB_WORKSPACE/Conversion/hicup2ncc --help
        $GITHUB_WORKSPACE/Misc/find_common_fragment_interactions.pl --help
        $GITHUB_WORKSPACE/Misc/hicup_capture --help
        $GITHUB_WORKSPACE/Misc/make_hic_array.pl
        $GITHUB_WORKSPACE/scribler/scribler
        $GITHUB_WORKSPACE/scribler/scribl_capture_ditag_sorter.pl
        $GITHUB_WORKSPACE/scribler/scribl_capture.pl
        $GITHUB_WORKSPACE/scribler/scribl_capture_region_breakdown.pl
                         
    - name: Download Test Dataset and genome index files and FASTA files
      run: |
        wget -nv https://www.bioinformatics.babraham.ac.uk/projects/hicup/test_dataset.tar.gz
        tar xvzf test_dataset.tar.gz
        wget -nv "https://genome-idx.s3.amazonaws.com/bt/GRCh38_noalt_as.zip"
        unzip GRCh38_noalt_as.zip
        wget -nv https://hgdownload.soe.ucsc.edu/goldenPath/hg38/bigZips/analysisSet/hg38.analysisSet.fa.gz
        
    - name: Run HiCUP
      run: |
        $GITHUB_WORKSPACE/hicup_digester --re1 A^AGCTT,HindIII --genome Human38 --zip hg38.analysisSet.fa.gz
        $GITHUB_WORKSPACE/hicup --digest Digest* --zip --threads 2 --index ./GRCh38_noalt_as/GRCh38_noalt_as --shortest 50 --longest 700 test_dataset/*.fastq
        
    - name: Check results
      run: |
         $GITHUB_WORKSPACE/Testing/hicup_checker --threshold 0 --expected test_dataset/HiCUP_summary_test_dataset_results.txt --new HiCUP_summary_report_* 
